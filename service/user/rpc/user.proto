syntax = "proto3";

package user;

option go_package = "./user";

message UserRequest {
  string id = 1;
  string name = 2;
}

message UserResponse {
  string id = 1;
  string name = 2;
}

service user {
    rpc saveUser(UserRequest) returns(UserResponse);
}